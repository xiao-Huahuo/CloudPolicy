# TODO
  - 主题色系:酒红珊瑚霓虹色系:
    深色底为酒红紫 + 深蓝黑渐变 (`#140c12` -> `#512334` -> `#15233f` -> `#09131f`),
    发光主色为暖橙珊瑚 (`#ff8f7a` / `#ffb76f` / `#ffdb64`) 对撞冷青蓝 (`#58cbff` / `#5fd1ff`),
    辅助点缀可保留少量荧光薄荷绿(`#80fab0`),后续补图和新 widget 尽量沿用这套“暖橙 + 冷青蓝”的霓虹科技色.
### 客户端UI
- [ ] 以每个页面为单位,检查响应式布局下手机端用户的体验,比如将图标sidebar放到底部,将文字sidebar变为从侧边上层展开且其他部位阴影,等.
- [x] 修复民生大厅的气泡区竖直宽度过窄看不到气泡的问题.
### 功能完善
- [ ] 为知识图谱添加更加直观的方式来进行展示,如树展示,广义表展示等.也应该是直接展示全部节点,且可以收缩和展开.
- [ ] 增加全方位的敏感词处理系统,优先考虑**AC自动机算法**(并写入readme算法亮点).
- [ ] 当前的agent实现了长短记忆,短期记忆是依靠context window,长期记忆依靠chroma存储聊天记忆,依靠sqlite存储聊天会话数据.那么
  当前的短期记忆是直接把上下文喂给LLM,还是滑动窗口,还是摘要压缩?
  - 尽量使用**摘要压缩+关键事实**的混合方法处理超出上下文的短期记忆.
  - 提升RAG的召回率,为长期记忆的RAG增加**混合检索**/**重排序**的功能:
    - 混合检索即RAG检索与关键词检索并行,比单路检索的召回率提高了15%~30%.
    - 重排序即在RAG检索后引入RERANK模型进行相关度精排,取前几条.
  - 切片逻辑:采用**重叠切片**,512~1024个token一个切片,重叠部分为128~256个token,以保证切片之间的连续性和上下文的完整性.
- [ ] 为发现页增加"刷文章"的体验,将全景政策广场放在资讯列表之上,资讯列表新增一个查看更多资讯按钮,就可以跳转到资讯列表页面,这个页面应该含有尽可能多的资源和图片,与极强的展示力.
- [x] 手机号登录,其附属功能:
  - 手机号验证码登录/注册(沙箱即可,不需要真正的短信服务,可以直接在后端生成验证码并返回给前端在前端的小框中展示,但是要sleep一点时间以显得真实).
  - 安全防护(图形验证码+频率限制)
  - 保留现有的邮箱登录基础作为可选,然后登录注册稍微改一下,首选手机号+验证码注册,可以无密码,也可以邮箱+用户名注册;登录的话,
    可以用手机号+验证码登录,也可以用邮箱+密码登录,也可以用用户名+密码登录.也可以通过忘记密码来找回密码(发验证码,或者用已绑定的邮箱发邮件,主
    打一个有什么就提供什么).然后在设置或者个人页面加一个绑定邮箱/绑定手机号的按钮,也是发邮件/发验证码.
注: AI在实现上述功能时,不仅可以按照上述功能描述来实现,还可以根据自己的理解和创意进行适当的调整和优化,让界面包含更多内容.只要不违背上述功能的核心需求即可,且页面和组件必须要接入全局的明暗切换状态管理.
### 数据/资源补充
- [ ] 填充巨量的初始化数据(部署阶段).
- [ ] 提供软件侧边栏的图标,以显得与众不同.
### 未来展望
- [ ] 增加多语言切换功能,用更企业的方法而不是一个json,应提供语言管理模块,且支持简体中文+繁体中文+英文+日文+法文等.

### 亟待完成
以下是演示视频需要修正成演示版的地方: 
- [x] 修复政策阅读页面乱码的问题.
- [x] 修复办事进度中心不可滑动的问题,并提供一个隐藏/显示已完成待办的拨动开关.
- [x] 让民生福祉大厅的民生反馈模块可以换页,条形模式10个一面,方块模式15个一面.
- [x] 把sidebar顶端的云枢观策四个字字体变成更好的字体.
- [x] 把登录注册页的云枢观策和首页的顶栏的云枢观策也改成上面的字体.
- [x] 将首页的"政务系统"这四个字改成"政策一体化平台".
- [x] 将发现页的"政务"改成"政策"
- [x] 让暗色系下主页知识图谱的外框架不要是白色,而是随着明暗和色系而变.这个页面里的返回按钮和导入会话按钮和图谱标题都要适配明暗切换.右上角的播报+收藏+导出这三个按钮也是.
- [x] 民生大厅的民生反馈需要有来源,即应该在政策推荐的每条政策的下面加上评论区和发表意见,并说具有审核机制(其实不需要有).还要更新相应的数据初始化内容,每个都加几条评论.
- [x] 修复Agent当上传了文件并解析后的一些问题:
  - [x] 修复无论用户在同一个提示词中提出了什么,只要上传文件,就只会回答"已生成图谱小窗，可直接查看知识图谱并切换原文。"的问题,Agent理论上应该根据实际情况考量.
  - [x] 修复上传后再次提问,会铁板回答固定格式的问题(如下):
    ```
    文档概览
    标题：需要完成怎样的流程办理呢
    类型：通用文档
    解析结论：文件内容已成功读取，但它更像资料/方案/说明文档，不适合用“办理通知”模板输出。
    摘要：需要完成怎样的流程办理呢
    关键信息
    办理流程：1=确定需求；2=收集必要信息和材料；3=选择办理方式（线上或线下）
    需要完成怎样的流程办理呢
    ```
  - [x] 增加让Agent获取个人画像和个人资料的tool.











