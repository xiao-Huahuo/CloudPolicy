# TODO
  - 主题色系:酒红珊瑚霓虹色系:
    深色底为酒红紫 + 深蓝黑渐变 (`#140c12` -> `#512334` -> `#15233f` -> `#09131f`),
    发光主色为暖橙珊瑚 (`#ff8f7a` / `#ffb76f` / `#ffdb64`) 对撞冷青蓝 (`#58cbff` / `#5fd1ff`),
    辅助点缀可保留少量荧光薄荷绿(`#80fab0`),后续补图和新 widget 尽量沿用这套“暖橙 + 冷青蓝”的霓虹科技色.
### 客户端UI
- [ ] 以每个页面为单位,检查响应式布局下手机端用户的体验,比如将图标sidebar放到底部,将文字sidebar变为从侧边上层展开且其他部位阴影,等.
- [x] 修复民生大厅的气泡区竖直宽度过窄看不到气泡的问题.
### 功能完善
- [ ] 为知识图谱添加更加直观的方式来进行展示,如树展示,广义表展示等.也应该是直接展示全部节点,且可以收缩和展开.
- [ ] 增加全方位的敏感词处理系统,优先考虑**AC自动机算法**(并写入readme算法亮点).
- [ ] 当前的agent实现了长短记忆,短期记忆是依靠context window,长期记忆依靠chroma存储聊天记忆,依靠sqlite存储聊天会话数据.那么
  当前的短期记忆是直接把上下文喂给LLM,还是滑动窗口,还是摘要压缩?
  - 尽量使用**摘要压缩+关键事实**的混合方法处理超出上下文的短期记忆.
  - 提升RAG的召回率,为长期记忆的RAG增加**混合检索**/**重排序**的功能:
    - 混合检索即RAG检索与关键词检索并行,比单路检索的召回率提高了15%~30%.
    - 重排序即在RAG检索后引入RERANK模型进行相关度精排,取前几条.
  - 切片逻辑:采用**重叠切片**,512~1024个token一个切片,重叠部分为128~256个token,以保证切片之间的连续性和上下文的完整性.
- [ ] 为发现页增加"刷文章"的体验,将全景政策广场放在资讯列表之上,资讯列表新增一个查看更多资讯按钮,就可以跳转到资讯列表页面,这个页面应该含有尽可能多的资源和图片,与极强的展示力.
- [x] 手机号登录,其附属功能:
  - 手机号验证码登录/注册(沙箱即可,不需要真正的短信服务,可以直接在后端生成验证码并返回给前端在前端的小框中展示,但是要sleep一点时间以显得真实).
  - 安全防护(图形验证码+频率限制)
  - 保留现有的邮箱登录基础作为可选,然后登录注册稍微改一下,首选手机号+验证码注册,可以无密码,也可以邮箱+用户名注册;登录的话,
    可以用手机号+验证码登录,也可以用邮箱+密码登录,也可以用用户名+密码登录.也可以通过忘记密码来找回密码(发验证码,或者用已绑定的邮箱发邮件,主
    打一个有什么就提供什么).然后在设置或者个人页面加一个绑定邮箱/绑定手机号的按钮,也是发邮件/发验证码.
注: AI在实现上述功能时,不仅可以按照上述功能描述来实现,还可以根据自己的理解和创意进行适当的调整和优化,让界面包含更多内容.只要不违背上述功能的核心需求即可,且页面和组件必须要接入全局的明暗切换状态管理.
### 数据/资源补充
- [ ] 填充巨量的初始化数据(部署阶段).
- [ ] 提供软件侧边栏的图标,以显得与众不同.
### 未来展望
- [ ] 增加多语言切换功能,用更企业的方法而不是一个json,应提供语言管理模块,且支持简体中文+繁体中文+英文+日文+法文等.

### 亟待完成
以下是演示视频需要修正成演示版的地方: 
- [x] !!!!!重要: 解决审图号问题!!!!!
  - 将公共数据分析页现有的阿里云GeoJSON格式中国地图更改为地图来源： (天地图 服务中心)[https://cloudcenter.tianditu.gov.cn/administrativeDivision] 提供的GeoJSON格式中国地图,审图号: GS（2024）0650号
- [x] 手机号一键登录应该要保证即使是未注册的手机号也要可以直接登录.
- [x] 替换软件图标favicon.icon.
- [x] 展示首页的占位符替换和文字正式化:
  - 02 轮播图组真正填充,标题下的说明文字修正,每页轮播图右下角的"可继续补图"以及其文字也修正.
  - 04 给每个优势和痛点的胶囊底色变成液态玻璃,并将文字变成白色;去掉下面中间的"待补主视觉"框.
  - 05 右下角的"这里可补一张理念主视觉"卡片文字修正.
  - 06 每个卡片的中间都太空,都要放图片,四个图片的存放地点: Agent 智能体 `web/src/assets/photos/main-examples/科研论文解读.png`; 公共数据大屏 `web/src/assets/photos/discover/slide2.jpg`; 政策扫描图谱 `web/src/assets/photos/main-examples/复杂系统设计规范.png`; 刷剧式资讯体验 `web/src/assets/photos/discover/news/discover-news-05.jpg`. 然后每个卡片下面的进入体验按钮上方的那个卡牌应该删除.
  - 07 将每个卡片改成透明底色,并加上业务描述流程,避免现在过于空旷.
- [x] 让首页和政策广场的图片在加载时不要等到用户滑到哪里才开始加载.
- [ ] 在政策推荐阅读页面的政策概览栏和中央文件栏的中间再加上一个新的"阅读区"栏,当点击左边的政策概览的一条时,文件展示会出现在这个阅读区栏,而不是让政策概览栏消失.
- [ ] 将政策发布中心发布了新政务文件后在右边显示的"我的文件"里面的每一条去掉左边的颜色带,并将其所有的胶囊标签变成文字.
- [ ] 解决办事进度中心里面点击一个待办之后待办文字消失但是框子仍在且不能删除的问题.
- [ ] 办事进度的新建待办的框子中,当点击了截止时间后不应该由用户自己手动输入,而是应该出现一个日期选择框.
- [ ] 解决政策推荐阅读页面中,文件不能点赞的问题.并在点赞旁边加一个收藏按钮.
- [ ] 删除管理员页面的公户IP地理分布区.
- [ ] 将主页上传区的三个按钮之中的"截图识别"换成"拍照识别".
- [ ] 把点击退出登录时的alert变成一个真正的消息框.
- [ ] 暗色系下:
  - 主页-示例的每一个卡片的胶囊的底色都应该随全局色系改变.
  - 登录注册页面的文本输入框里面现在会出现黑色长方形,应该把输入框的这个矩形给变成透明底色.
  - 云小圆页面的标题和副标题下的两个胶囊的文字应该变成白色,且思考过程框和聊天框的颜色应该是随色系决定,而不是简单的浅红.
- [ ] 在doc/作品演示演讲稿.txt中书写作品演示视频全流程,5分钟左右,要求的是:遍历项目的主要功能,突出项目的技术亮点,亮点处可选描述本产品解决的难点.















