### 历史TODO
- [x] 提供头像上传和持久化.
- [x] 增添无数据库时自带的数据,便于数据可视化.
- [x] 主页-示例对应的示例图片资源以及示例接口.
- [x] 编写设置类,将用户设置持久化.
- [x] 文件格式扩充:增加支持pdf,docx,xlsx的传递的接口(form-data).
- [x] 文件格式扩充:增加支持截图解析接口和扫描型pdf的接口(OCR).
- [x] 增补用户字段,增添管理员身份,管理员拥有不同的界面逻辑,应该统计多用户的各项数据(数据可视化占大头).
- [x] Todo List功能的接口.
- [x] 加入爬虫逻辑,提供URL解析与分析接口.(Rate Limiting（限流）,Redis缓存)
- [x] 文字转语音(TTS)的播报功能.
- [x] 编写明暗切换逻辑.
- [x] 管理员页面的编写.
- [x] 个人收藏功能.
- [x] 办事进度中心 Todo List页面.支持用户直接从对话生产出Todo List,支持用户确认保存Todo或者废弃Todo.
- [x] 补充完整数据分析页面的剩下的数据分析表.
- [x] 美化"我的"页面,提供更多功能的接口.
- [x] 编写"设置"页面,提供更多的用户个性化设置,如隐私预处理等.
- [x] 将整块页面切割为三个部分,左边三分之一少一点,中间三分之一多一点,右边三分之一少一点.
    左边的:
        上5/12:放置节省时间曲线图,提供曲线图和柱状图两个模式,模式在右上角可切换,默认曲线图.
        下7/12:放置通知类型分布的南丁格尔玫瑰图,每个玫瑰叶片旁边有一段双折线,其中一段必须水平(放置通知类型名),另一段接在玫瑰叶片上.
    中间的:
        上3/4:放置高频材料统计点云图(就是一个个的圆点,圆点下面放置灰色字写这个词),点的大小取决于这个词的统计数字.右上角可选择饼图,点云,词云.
        下1/4:放置用户节省时间卡片,分为堆叠的两个卡片,左下卡片在上,右上卡片在下.左下卡片放置平均节省时间,右上卡片放置总计节省时间.
    右边的:
        上1/3:放置TOP5所需材料词频横向柱状图,按照所需材料词频排序,高的在上.每一条左边写材料名,右边是长胶囊形,再右边则写词频.对于上下间距,应该上下能展示top5,余下的可以点击下方的小箭头展开列表,展开时这个右边块的其他部分消失,只显示这一个栏.
        中1/3:通知难度分布柱状图.从左到右有9个柱子,分别是语言复杂度的低中高,办理复杂度的低中高,风险等级的低中高.每个柱子上面写这个难度的会话数量.
        下1/3:最近会话历史,显示最近一次会话的概要和内容.
- [x] 丰富主页信息密度，原本的拖拽式主页转移到"文件处理"中。具体改动：
  - 采用多模块平铺布局,右边加入红头文件栏(滚动卷轴),在栏目顶部展示最新中央文件标题,中间放置热点聚类分析点云图,下方直接展示最新中央文件的内容.左边加入时事热点栏,左边顶部采用轮播图展示时讯,中间展示top10时事资讯,点击后自动跳转链接.编写爬虫以实现资源的获取.
  - 在示例图片栏下面再加上一个热点资讯图片栏,爬取热点资讯的图片和资讯作为当下热点?采用横条式,左边图片右边资讯.
  - 重构文件扫描后的对比视图,采用双栏预览机制,左边展示原始文件文字(如果是pdf则展示红头文件pdf,使用pdf.js,支持高亮和缩放),右边上方"流式输出"AI思考过程,思考结束后展示AI结构化之后的清单.在最底部增加输入框,便于提供追问功能(之后转接内置Agent).输入框上方提供多版本即时切换的按钮.
  - 文件扫描后,可以点击对比视图右上方某个按钮显示关键词前后对比页面.此时两边都是纯文本,关键词使用黄色标记,且使用虚线进行对应(Diff 算法,语义对齐).
  - 在对比视图的左边展示当前文件与过往通知和过往政策(使用RAG)之间的关系图谱(Knowledge Graph).
  - 新增的所有爬虫模块都被一个老年人按钮统一控制状态,可以切换高信息密度和简约模式.
- [x] 文件处理页修改为"发现"页:
  - [x] 顶部是轮播图和右边的描述文字,顶部右边有一个区域展示"今日政务概况".
  - [x] 中间是类似于[央视网](新闻频道_央视网(cctv.com))的砖块式布局,每个砖块存储一个功能或者链接,如:极速识别,语义实验室,流程可视化,政策对比工具,智能日历导出,适老语音版
  - [x] 下方则是新闻列表(支持列表浏览和卡片浏览),每条新闻标注"AI 复杂度指数",表示对新闻做深度加工.
  - [x] 右下方提供一个热点资讯栏,存放的是和主页相同来源的top5热点文件.
- [x] 搜索页的编写.
- [x] 将整个软件的配色更改为红灰主题配色:
  - header红灰渐变,sidebar改为红色主题旗帜形渐变波浪,主页的时事热点和中央文件和资讯列表和热点文件top5的顶栏变灰,标题变白;
  - 发现页的所有除了功能中心之外的widget在无图时默认色彩变为红色或者红色渐变或者红色的周边颜色,功能中心的砖块默认配色变为蓝色和蓝色周边的颜色.
  - 功能中心的砖块式布局的圆角去掉,距离减小,将所有的砖块承载在一个"传送带"上,传送带从左边向右边缓缓滚动,且右边消失的从左边出现.
  - 发现页的顶部新加入搜索框,放入的是搜索页当删除了搜索词后的那个搜索框以及它下面的"热门""最新政策""时事热点"栏目,并添加一个拨动开关来配置是否显示这三个栏目".原来的轮播图被挤到左边去.
  - 数据分析页的所有数据分析图表所在的卡片式布局变成主页的时事热点那样的扁平式布局,且将所有图表的配色转变为红黄蓝灰黑之类的配色或者渐变色,且动态效果不变.
  - 会话历史的复选框被勾选后的颜色改为红色底色白色对勾.
  - 个人中心和更多设置的所有卡片式布局也改成主页的时事热点那样的扁平式布局.为个人中心添加一些内容,显得不那么空白.
  - 将主页的原来的文件上传框以及示例图片栏进行适配,也修改为更符合现在的UI布局的布局.
  - 修复资讯列表中,文章内容带有<p style>等HTML标签等问题.
- [x] 发现页轮播图背景图片（可选，当前使用渐变色代替）：
  - `web/src/assets/photos/discover/slide1.jpg` - 智能解析主题图
  - `web/src/assets/photos/discover/slide2.jpg` - 多版本改写主题图
  - `web/src/assets/photos/discover/slide3.jpg` - 数据分析主题图
- [x] 将发现页轮播图背景图片(web/src/assets/photos/discover/)真实渲染上去,代替掉当前的渐变色.
- [x] 登录注册页从原来的圆角水晶蓝色渐变改为更小圆角的红黑渐变.给登录页面出现时的标题增加"一个一个字母浮上来"的动效.
- [x] 将左下角齿轮更改为"更多",悬停时展示栏目有:
  - 关于(包括关于本平台,关于我们).
  - 权限设置(管理员可以自我设置为用户,用户需要在所有的管理员中选择一个去申请提高权限,需要增补邮件系统,使用真实邮箱进行邮件传递,管理员降级和用户提交申请和用户申请通过时都要发送邮件)
  - 接入API(暂时搁置,等内置智能体开发完成后再接入)
  - 常见问题(瞎编)
  - (红色按钮)不要点这个!,当用户点击这个时自动跳转到[原神](https://ys.mihoyo.com/)页面.
- [x] 未登录时在sidebar的最下面的栏目下面放一个widget,"登录即享"...,写四条,下面是一个胶囊形红色登录按钮.
- [x] 主页-中央文件的词云图的每个词应该可点击,点击后自动调用搜索引擎搜索这个词.
- [x] 在header右方添加一个明暗切换开关.
- [x] 将数据分析-通知类型分布的玫瑰图动效改编为顺时针生长的动效.增大"高频材料分析"的点云中每个点的大小,让点充满高频材料分析的区域;词云也应该覆盖满区域,且点击词云的每个词后都在内置搜索框中搜索这个词.
- [x] 为管理员控制台提供更多数据可视化,并针对管理员增加更多特有的数据分析接口(针对多用户和所有用户总体数据的分析,如多线程的所有用户的解析数量变化曲线,放在右上角),在管理员的数据分析页面中需要有:
  - 节省时间趋势成为两条曲线(两组柱子),一蓝一红,蓝色代表针对所有用户的某种节省时间走向分析?.
  - 通知类型分布,高频材料分析,核心材料top5,以及通知难度评估均提供管理员个人分布和所有用户分布的切换.
  - 管理员的节省时间卡片不同于用户,应该有4个,分别个人平均节省时间,个人总计节省时间,所有用户平均节省时间,所有用户总计节省时间.
- [x] 管理员页面的用户管理应该显示各用户头像.
- [x] "我的"页面的累计为您节省时间不要使用随机数.
- [x] (红色按钮)不要点这个!,当用户点击这个时自动跳转到[原神](https://ys.mihoyo.com/)页面,并开始下载[原神](https://ys-api.mihoyo.com/event/download_porter/link/ys_cn/official/pc_backup316).
- [x] 缩小header右边的明暗切换开关.
- [x] 解决主页扫描文件后内容分布左右不均匀的问题.(解决LLM生成内容量太少的问题),添加文件解析后用流程图展示功能.
- [x] 解决现在数据分析无法显示以及管理员页面无数据的问题.
- [x] 解决发现页面的数据加载不上去的问题.
- [x] 规范化前后端各文件名和函数名称
- [x] 将管理员页面的个人和全体的点云图变为柱状+曲线联合图;
- [x] 遍布整个后台的日志管理.
- [x] 修改"节省时间"计算逻辑,防止有些文件的节省时间超过三十分钟.
- [x] 增加会话历史查询接口的排序方式查询逻辑.
- [x] 爬虫逻辑在已加入Redis的情况下,加入限流业务.
- [x] 解决正常文档上传时能解析(有解析记录)但文件处理失败: Request failed with status code 500问题.
- [x] 修复会话历史中点击目录没反应,点击导出显示导出失败的问题.
- [x] 将管理员用户的**数据分析页面**(不是管理员页面!!!!)进行以下修改(AI可以去直接修改后端逻辑和接口):
  - 节省时间趋势从现在的红线变成同步绘制红蓝双线,蓝色代表全体用户的节省时间趋势;柱状图也一样.
  - 通知类型分布,高频材料分析,核心材料top5,以及通知难度评估均提供管理员个人分布和所有用户分布的切换.
  - 管理员的节省时间卡片不同于用户,应该有4个,分别个人平均节省时间,个人总计节省时间,所有用户平均节省时间,所有用户总计节省时间.
- [x] 将登录注册页面变为红灰配色.
- [x] sidebar添加一个"我的收藏".
- [x] 真实解决OCR解析扫描型pdf和图片后前端却显示未知事项的问题(能够解析出文本json但显示未知事项且无内容,解析后可以选择多版本转述且无问题),如下:
- [x] 解决点击截图后出现两次文件选择,第一次只能选择非图片,第二次才能上传图片格式的问题.
- [x] 基于爬虫实现URL上传功能以及相应的异常处理措施.
- [x] 提供恢复对话功能,每次对话后将对话保存到一个json文件,可以在历史记录中点击对话来恢复,也可以点击历史记录每个对话的文件夹来在文件资源管理器中显示.
  - 在主页解析完成文件后右上角添加一个导出按钮.在主页和会话历史右上角分别提供一个导入按钮,导入对话的json来重建对话并加入历史记录.
- [x] 现在修复点击本地上传后选择文件后会再次出现文件选择器的问题;修复点击截图后可以选择文档的问题,它应该只能选择图片格式.给
  URL上传单独添加一个小型悬浮输入框,而不是像现在这样用log的输入框.然后添加页面逻辑:当选择好了上传文件后,左边的时事热点
  和右边的中央文件这两个栏目都分别平移出页面,中间的所有布局都扩展到整个页面.点击退出时再返回到初始页面.将时事热点的竖直
  方向变为原来的下面2/3,上面空出的1/3放置最近解析的历史记录卡片以及它的详情,并提供恢复历史记录的功能.现在给所有的历史记
  录都提供一个五角星,点击后变黄并表示收藏.
- [x] 加入真实邮件系统,补完注册时验证邮箱的逻辑.
- [x] RAG(检索增强生成)的引入.
- [x] 引入向量数据库(ChromaDB),预存入国家政务规则文件,解决内置Agent的"幻觉"问题.
- [x] 从调用LLM的API,到制造软件内置智能体,利用LangChain制造软件内置智能助理Agent.
- [x] 利用内置智能体去实现追问功能.
- [x] 混合驱动架构(Hybrid):简单的统计交给本地 jieba 算法,复杂的意图理解和任务调度交给 Agent,端云结合.
- [x] Agent根据用户习惯推测用户可能的困难,弹出对话框提示用户如何点击.
- [x] 智能体页面,需要完成软件内置智能体.带有智能体思考过程.
- [x] 将数据分析页面(不是管理员页面)进行改造:将高频材料分析切成四块,高频材料分析放在右下角那一块,然后上面的两块合并放
  一个含有RAG数据体现的曲线表或者折线表(可切换),左下角那一块放一个你觉得很有价值的图上去比如向量散点图评估模型.可以编写
  所有所需的接口和业务.尽可能包含更多的数据.对于管理员,这些所有新增的图都应该配备所有用户和管理员个人的数据切换.然后,对
  于普通用户,这个数据分析页面则只能显示用户个人的数据.
- [x] "实时"逻辑:管理员可以实时监测所有用户的解析数量变化,可能要引入多线程?.
- [x] 解决现在上传文件时发送会先发送用户在文本框里面的文字,然后再发送文件解析的分开发送问题.应该把文件和用户输入一起发送,将
  文件显示在用户的气泡上面.
- [x] 给智能体回复渲染出真实的markdown.(使用`markdown-it`)
- [x] 实现Agent的记忆:
    - 短期记忆:对话历史,可以通过会话历史接口获取,也可以在智能体页面展示.
    - 长期记忆:用户的偏好设置,用户的常用材料,用户的常见问题等,可以通过用户设置接口获取和修改.
- [x] 编写配色的全局切换器,且和header的明暗切换按钮以及设置中的整体明暗主题的状态进行一致化调节.为后来增加第三色系做准备.
- [x] 让"我的"界面未登录时页面中间显示的立即登录/注册按钮点击后显示的为Home的登录注册界面,而且样式必须相同;
- [x] 在未登录情况下,在主页之外的页面点击header的登录按钮和sidebar里面的"登录即享"登录按钮是无反应的,应该统一出现Home的登录注册界面.
- [x] 将"发现"页面变成单独的一个页面,sidebar点击后进入这个单独的页面,此页不在sidebar和header锁定下的布局中,而是独立作为无边框的网页.
- [x] 解决登录注册页面的输入框在输入文字后会出现与背景不合的矩形的问题.

- [x] 明暗切换的完善:基于配色的全局切换器的前提,在暗色系下,
  1. 主页的"导入会话"按钮应该变暗,文字变白.主页的"最近解析""中央文件""时事热点"这几个栏目的背景应该变暗,文字变白."最近解析"的所有条目应该变为像中央文件的第一名白色那样的粉红色,"中央文件"的条目也应该在保留第一名浅红色的前提下,第二名第三名变成较深的浅红,其热点聚类分析的词云图背景应该变黑,其他的所有字要么变白要么变灰白."时事热点"栏的轮播图不变,下面的栏目则进行适应的变化."当下热点"的所有栏目都应该变为灰底白字."实例"图片栏的底应该变为更深的红色和灰色.
  2. headbar的搜索框里面的矩形变成了与搜索框不适配的样子(黑色矩形),应该明暗不变.发现页面的搜索框也是.
  3. 发现页面的搜索框下面的"热门-最新政策-时事热点"的栏目背景应该变暗,文字变白."今日政务概况""资讯列表""热点文件top5"都应该像主页那样进行暗黑调整.
  4. 数据分析页面的"RAG命中趋势"的"曲线/柱状"切换按钮的底色应该是与卡牌相容的黑色.同样的还有"通知类型分布"的"个人/全体","节省时间趋势"的"曲线/柱状","高频材料分析"的"饼图/点云/词云".
  5. 数据分析页面的通知类型分布的玫瑰图每一片的边缘都应该变为无,且文字应该为白色.高频材料分析的点云图的每个点的文字应该变白,饼图边缘都应该变为无,且文字应该为白色.个人平均节省时间和全体用户平均节省时间的卡片底色应该分别变为更深的粉红和更深的浅蓝.相应的文字应该变为白色.
  6. 智能体页面的背景,标题,文字,聊天框,聊天气泡等都应该变为相应的具有区分度的黑色或者灰色,文字变为白色或者灰白色.
  7. 会话历史页面的"导入会话"按钮应该变成和主页的导入会话按钮一样.所有会话的标题都应该为白色,当鼠标浮上去时则平滑为红色.
  8. 我的收藏页面的所有收藏都应该变为暗色背景白色文字.
  9. 办事进度页面的新建待办按钮点击后出现的待办框应该将背景变为暗色,而输入框变为白色,待办框标题变为红,而所有输入框的placeholder文字颜色不变.
  10. 设置页面的所有标题,除了注销账号之外,都应该变为白色.整体明暗主题的切换按钮应该变为黑色底红色按钮.
- [x] 全面调整chat_message的结构化字段,完全放弃无用的结构化通知提取(因为普通AI也能做到),而是转向全面的知识图谱可视化显示,动态标签,动态json,返回的内容完全由LLM决定,并设计 三维知识图谱球+二维知识图谱+矩形文本 这三种可视化方法.
    具体实现思路如下(可以对字段进行必要的补充或者修改):
    ``` 
    1. 模型结构定义 (Pydantic / SQLModel)我们需要将返回内容解构为三层：元数据层、知识图谱层（点边数据）、展示配置层（控制 3D/2D 渲染）。Pythonfrom typing import List, Dict, Any, Optional
    from pydantic import BaseModel, Field
    
    class KGNode(BaseModel):
        """知识图谱节点：支持动态标签"""
        id: str = Field(..., description="唯一标识符")
        label: str = Field(..., description="节点显示名称")
        type: str = Field(..., description="节点类型，如：主体、权益、时间、限制条件")
        importance: float = Field(0.5, description="重要程度 0-1，决定 3D 球体中的节点大小")
        properties: Dict[str, Any] = Field(default_factory=dict, description="LLM 决定的动态属性/标签")
    
    class KGLink(BaseModel):
        """知识图谱边：定义逻辑关系"""
        source: str = Field(..., description="源节点 ID")
        target: str = Field(..., description="目标节点 ID")
        relation: str = Field(..., description="关系描述，如：属于、前置条件、排他关系")
        logic_type: str = Field("positive", description="逻辑性质：positive(正向), negative(约束/冲突)")
    
    class VisualConfig(BaseModel):
        """可视化参数：指导前端渲染引擎"""
        focus_node: Optional[str] = None
        initial_zoom: float = 1.0
        # 文本锚点映射：点击图谱节点后，矩形文本框自动高亮并滚动的索引
        text_mapping: Dict[str, List[int]] = Field(default_factory=dict, description="节点ID对应原文的[start, end]索引")
    
    class ChatMessageBase(BaseModel):
        """重构后的核心响应模型"""
        content: str = Field(..., description="LLM 生成的自然语言简述或经过标记的原文")
        
        # 核心：动态图谱数据
        nodes: List[KGNode] = Field(default_factory=list)
        links: List[KGLink] = Field(default_factory=list)
        
        # 扩展：完全由 LLM 决定的动态 JSON 负载
        # 比如：LLM 认为这个政策有特殊的计算公式，可以直接放在这里
        dynamic_payload: Dict[str, Any] = Field(default_factory=dict)
        
        # 指导前端三维/二维切换的配置
        visual_config: Optional[VisualConfig] = None
    2. 后端核心实现思路A. 提示词工程 (Prompt) 的重塑在 analysis_agent.py 中，你需要强制 LLM 输出上述格式。不再问“政策是什么”，而是命令它“构建拓扑”：“请将该政策解析为非线性的知识网络。识别所有权利实体与义务边界，并评估其关联强度。输出必须严格符合 JSON Graph 格式，为重要节点标注 importance。若存在因果逻辑，请在 links 中标记 logic_type。”B. 矩形文本 (Rect-Text) 的关联实现为了实现点击图谱节点、原文高亮的“穿透效果”，后端在生成 text_mapping 时，需要计算节点在 content 字符串中的偏移量。实现技巧：在 content 中使用特定的 HTML 标签或 ID 包裹实体，例如 <span id="node_1">专精特新企业</span>，前端解析 VisualConfig 时直接定位 DOM。3. 前端可视化对接方案在 Vue3 中，你可以根据 ChatMessageBase 返回的数据，同时初始化三个视图：可视化组件技术实现核心逻辑三维图谱球3d-force-graph (Three.js)遍历 nodes，将 importance 映射为 nodeVal。使用球状力导向布局，展示政策的全局复杂度。二维逻辑图antv/g6 或 D3.js渲染 links 中的 logic_type。如果是 negative（约束条件），线段渲染为红色破折号；如果是 positive，渲染为带箭头的实线。矩形文本框Markdown-it + Custom Highlight监听图谱组件的 onNodeClick 事件，获取 text_mapping 中的索引，调用 window.scrollTo 实现联动。 
    ```
- [x] sidebar 提供预览模式(当前)和图标模式(仅剩图标,变窄,胶囊形,顶端的主图标和设置和更多也仅剩图标),切换的开关放在header上,且提供平滑切换.在header左边提供一个三条横线图标的按钮(图标sidebar模式才显示),点击后在侧边平滑展示抽拉的sidebar,sidebar顶端的软件图标左边也要加一个这个按钮用户隐藏.默认应该是图标模式.
- [x] 点击sidebar顶端的主图标后跳转到主页,无论什么模式.
- [x] 主页的中间的文档解析改为居中的特别字体的"看见政策",副标题改为多模态+可视化+富呈现.
- [x] 二维知识图谱中,根据球所属的深度确定其显示的颜色,按照顺序分别是红黄绿蓝紫橙青(大于七层时循环);增加交互:
  - 球需要可以自由拖动,拖动时,图谱的中心球位置不变,其他球按照力学规律排斥.
  - 球需要可以点击,单击后,非此球的子孙球的球颜色平滑变浅,文字消失,此球高亮(颜色不变,亮度增加,细微的光圈),子孙球稍微增加亮度.点击其他地方恢复.
  - 图谱右边添加一个图谱操作栏,提供一个"深度筛选"功能,即选择深度(从0到最高深度),选择后可以使这一深度的所有球高亮.
  - 在图谱操作栏的顶部提供一个白色的胶囊搜索框,可以在所有的球中按照前缀搜索,搜索的结果在图谱中高亮.
- [x] 三维图谱功能变灰,显示"即将推出".
- [x] 矩形文本功能:像YAML接口协议在浏览器中展示一样,按照深度,深度为1的要点每一条都作为一条,展开后可显示它的子条目,子条目也可以展开显示孙条目,条目框应该为白色加边框,边框颜色和二维图谱的深度颜色算法相同.
- [x] 在矩形文本右边增加一个查看原文功能,点击后直接展示原文(无论是什么格式,展示的是扫描后得到的原文),查看原文页面的右上角提供一个查看原文件按钮.去掉"原文件参考"和他的链接,把链接接入到这个按钮.
- [x] 统一UI,具体如下(必须接受全局明暗切换:Light模式/Dark模式,文字根据实际决定切换成什么):
  - 按钮统一:采用胶囊按钮(红/红),带有深红色边缘显得有层次感:
    - 主页-最近解析的刷新按钮
    - 主页-多模态上传的三个按钮
    - 发现页-搜索框变为胶囊形,其搜索按钮采用胶囊按钮
    - 发现页-热门的所有标签
    - 会话历史-导入对话按钮
    - 我的收藏-查看详情按钮.  将取消收藏按钮变为五角星.取消收藏后插一个五角星弹一下平滑消失的动画然后再取消收藏.
    - 办事进度-右上角的新建待办按钮. 将状态选择的三个按钮变为胶囊
    - 管理员-右上角的刷新按钮.
- [x] 将主页-最近解析栏作为抽屉栏目,默认隐藏,左侧提供一个开关用于平滑抽拉.
- [x] 将主页-时事热点栏移动为中央文件栏左边的新栏.
- [x] 管理员页面顶部的"注册用户-总解析次数-活跃用户"三个widget变为浅黄-天蓝-浅绿的圆角,无边缘,且分别爱左边增加一个更深颜色的圆,圆里面放相应更深颜色的图标.
- [x] 延长header的搜索框,在搜索框下方展示发现-热门下的几个tag(由后端提供).
- [x] 将主页-时事热点栏变成中央文件栏的位置的另一个选择,并提供切换开关.
- [x] sidebar的图标模式,应该让所有图标竖直居中排列,且sidebar呈现胶囊形,然后header的最左就可以延伸到页面的最左边.将最底下的设置图标也移动到中间这群图标的最下面,去掉多余的长度.修改之后,页面显示不应该和header那样完全占据页面宽度,而是应该保持左边隔离sidebar的样子,以免被sidebar遮住.
- [x] 将主页+搜索页(正中间补充一个)+数据分析页+智能体页+会话历史+我的收藏+办事进度中心+个人中心+系统设置+管理员页面 的标题都改成主页中间"看见政策"的样式(不是改成这个标题),且被全局调控以便于明暗切换和颜色切换.
- [x] 将主页的文件上传区的三个按钮的形状从胶囊变成半径相等的圆形(保持边缘的暗色样式).半径以最长的为主.
- [x] 主页-最近解析栏在未滑出时应该完全隐藏.
- [x] 给主页右侧的中央文件栏提供一个和左边的最近解析抽拉按钮一样样式的抽拉按钮,可以平滑的抽拉.
- [x] 解决header的搜索框下的标签显示为`[object Object]`的问题
- [x] 添加更多权限:
    - 管理员: 可以管理普通用户，审核上传的政务文件，回复用户留言等。
    - 认证主体(新): 可以上传政务文件，查看相关数据分析，但不能管理其他用户。
    - 普通用户: 可以浏览政务文件，参与民意评议，但不能上传文件或管理其他用户。
- [x] 民意评议大厅页面（公开可见）: 将全站用户对各项政策的落地评价、智能解析纠错和办事留言进行公开展示，形成真实透明的社会反馈信息流。认证主体登录后额外可见专属视图：查询自己提交的所有政策及对应的用户民主反馈。
  - 顶部区域：左边三分之二为轮播图，右边展示热门最新政策top5，以用户评价数和浏览数和点赞数进行综合计算，提供强制置顶功能，并提供一个展开链接展示排序后的所有的政策。
  - 中间区域：以大小和位置不固定的“动态上下浮动气泡词云”形式展示最热门的词语，和主页右边的栏目的词云计算逻辑相似，但是点击词语后展示的是政策搜索结果。
  - 下方区域：分条展示实时最新政策，随着用户滑动渲染，理论上可以无限下滑直到政策的最后一条。提供条形模式和砖块模式切换，提供刷新按钮。每条政策还要展示其发布者信息和最热门评价信息。
- [x] 发现页面改编为"全景政策广场"，集中展示各方认证主体上传的最新政务文件和惠企政策。将静态国家政策数据改变为真实的认证主体上传的政务文件,结合爬虫获取的时事热点资讯,联合展示.
- [x] 认证主体专属的数据分析页面: 认证主体可以查看自己上传的政务文件的解析数据分析,如解析数量,用户反馈等.并且可以和所有用户的总体数据进行对比分析.
- [x] 认证主体专属的政策发布中心页面: 可以上传标准化的政务文件,上传后需要管理员审核通过才能在全景政策广场展示.审核通过后这个文件就会出现在全景政策广场的最新政务文件中,并且会被智能解析和纳入数据分析的统计中.
- [x] 认证主体数据分析页面增加可视化内容（认证主体专属）:
  - 发布数据追踪: 展示各项文件的浏览量、被调用解析的频次以及群众的具体反馈评价，帮助发布者了解政策下达的实际穿透率。
- [x] 管理员数据分析页面增加可视化内容（管理员专属）:
  - 民意评议分析: 展示用户对各项政策的评价分布、智能解析纠错的反馈情况以及办事留言的内容分析，帮助管理员把握群众对政策的真实态度和需求。
  - 用户IP地理分布: 展示用户IP地址的可视化分布的可交互可缩放中国地图，帮助管理员了解政策触达的地域覆盖情况。(需要增加用户IP字段)
- [x] 制作展示页面路由组，带有新的header，无sidebar，包含以下页面：
  - 含有极其丰富的平滑的随着用户下滑的动效的展示效果的高级感展示首页。需要带有最底部的链接区。
  - 发现页面：复用现有发现页面组件，仅替换为展示路由组的header（不重复实现页面逻辑）。
  - 公共数据展示大屏（蓝色科技感流动实时动效大屏幕）。
  - 我的（直接导向”我的”路由）。
- [x] “刷剧”体验: 根据用户职业字段+浏览历史进行标签匹配推荐，用户可上下滑动像看小说一样浏览政策文章。推荐逻辑：优先匹配用户职业相关标签，其次按浏览历史中的高频标签补充，兜底展示热门文章。
- [x] 制作一个sidebar两个模式平滑切换的动画(像液体一样),header也跟着平滑切换.
- [x] 展示首页/showcase底部的免费注册按钮导向的view/Register.vue和Login.vue里面的登陆注册界面做的非常好,标题的动效也做的很好,将这些组件原封不动的搬运到components/Home/里面的登录和注册表单中.
- [x] 展示首页/showcase的动效优化: 顶部和中部都应该实现和和下部那样的用户滑动上浮效果,只有用户滑动到那个地方才展示上浮动效.
- [x] 将发现页面的背景变为和展示首页那样的红蓝渐变.将发现页面的功能中心滚动栏变成不滚动的参差错落砖块墙效果,就像[央视新闻](https://tv.cctv.com/index.shtml)那样.每个砖块都要加上反光效果和边缘效果.
- [x] 将发现页面的轮播图变为涵盖整个showcaseheader之下页面上部的全景实时滚动栏,不再是一张一张切换,而是照片无缝滚动.搜索栏则缩小并放在轮播图上方,且点击搜索栏之后才显示DiscoveryHome.今日政务概况的栏目则背景变成渐变灰底色,贴在轮播图的右边.
- [x] 将主页的文本上传框限制左右最大宽度,并增加上下宽度.并增加从左到右逐渐显示,按钮一个接一个显示的动效.将标题稍微向右移动一些,要强制放在文本上传框之上的正中间.删除主页的当下热点栏.
- [x] 纠正数据分析页面的节省时间卡片变成条状的问题.
- [x] 为会话历史页面增加列表从上到下加载的动效,且初始只加载30条历史,滑动到下面后加载后续的.
- [x] 为我的收藏页面的收藏和待办页面的待办都加上逐个显示的动效.
- [x] 将民意大厅中间的词云每一个词都放在上下浮动的有光滑质感的力学排斥的气泡内,并根据词云的权重决定气泡的大小.将词云背景的widget延伸到两边.
- [x] sidebar重排序: 主页-政策推荐-发现-民意大厅-认证发布(包含: 政策发布-发布追踪)-办事进度-ClearFlow智能体-我的收藏-会话历史-数据分析-我的-管理员,并将发现页面的重定向改为包含showcaseheader的那个发现页.
- [x] 修改发现页面:将轮播图延展到页面上1/3,左右填满,轮播图从连续轮播改成原始的单页切换轮播.搜索框宽度减小一半,长度变成页面水平的四分之一,变成透明偏灰,嵌在轮播图上(而不是上方,是里面的上面),只有在点击搜索框时才在搜索框的下面显示出热门和最新政策,且要随着用户输入进行前缀搜索并展示搜索内容;右边的那个栏变成透明灰底,嵌入(而不是放在)在轮播图的里面的右边.
- [x] 将发现页面的功能中心的所有砖块保持样式不变,布局变成不同大小的砖块的彼此切合的砖墙布局.
  - 什么是砖墙布局?就是说,假设横竖分别是4*12,那么[0:1,0:3]放一块,[0,4:5]放一块,[1:2,3:5]放一块,像这样的,参差不齐但是遵循同一个网格的,就是砖块墙布局.
- [x] 将发现页的顶部showcaseheader改成透明的,下滑时底色变白文字变白的逻辑不变.此逻辑只对发现页生效.
- [x] 将民意大厅的气泡和文字变小一点,且承载气泡的widget应该延伸到两边.
- [x] 修理登录注册界面的问题,现在登录注册界面有两个标题,且登录按钮被挤占了.你要做的应该是全盘继承views/Register.vue和Login.vue,将他们的widget全部直接应用于components/Home里面的登录和注册组件,而不是缝缝补补.
- [x] 修理管理员无法查看管理员页面的问题.
  - [x] 补充好各个地方所需的真实图片,将需要的图片列举在下面:
      主页-示例的占位图片*3,后续扩展成*9
      发现页面的轮播图*3
      发现页面-资讯列表的占位图片
      民意大厅的轮播图*5
      首页-为什么选择我们区域的六个图片
      首页-各个合作伙伴的图标
  图片填充清单(位置/命名/大小)：
    1) 主页-示例占位图（现 3 张，后续扩到 9 张）
       放置位置：
       - 页面/组件：web/src/views/Home.vue -> .examples-grid -> .example-card -> .breakout-image img
       - 资源目录：web/src/assets/photos/main-examples/
       命名：
       - example1.png ~ example9.png
       大小：
       - 前端显示：约 140px 宽（高度自适应），卡片图片区高度 120px
       - 建议源图：840x600（4:3），最小不低于 560x400
    **事实操作**:在main-examples/中全部改为了png文件.命名方式自由化.应该将逻辑进行修改,读取所有的png格式图片然后显示.
    2) 发现页面轮播图（3 张）
       放置位置：
       - 页面/组件：web/src/views/DiscoveryHome.vue -> .panorama-hero -> .hero-slide（background-image）
       - 资源目录：web/src/assets/photos/discover/
       命名：
       - slide1.jpg, slide2.jpg, slide3.jpg
       大小：
       - 前端显示：容器高度 clamp(260px, 33vh, 420px)，宽度自适应
       - 建议源图：1920x1080（16:9），最小不低于 1280x720
    **事实操作**:填充了五个jpg.应该在逻辑里面加上后两个jpg.
    3) 发现页面-资讯列表占位图
       放置位置（按当前页面结构预留）：
       - 页面/组件：web/src/views/DiscoveryHome.vue -> 资讯区 news-list-view / news-card-view（建议在卡片头部区域补图）
       - 资源目录建议：web/src/assets/photos/discover/news/
       命名建议：
       - discover-news-01.jpg ~ discover-news-20.jpg
       大小：
       - 卡片头图建议显示区域：约 100% x 60px（可按 .nc-header 扩展）
       - 建议源图：640x360（16:9），最小不低于 480x270
    **事实操作**:已填充,且多放了了两个.应该替换掉占位符.
    4) 民意大厅轮播图（5 张）
       放置位置：
       - 页面/组件：web/src/views/PublicOpinionHall.vue -> .carousel-area -> .carousel-slide（顶部轮播）
       - 资源目录建议：web/src/assets/photos/opinion-carousel/
       命名建议：
       - opinion-slide-1.jpg ~ opinion-slide-5.jpg
       大小：
       - 前端显示：轮播区高度 280px，宽度为 top-section 左侧 2/3
       - 建议源图：1600x700（约 16:7），最小不低于 1200x525
    **事实操作**:已填充.应该替换掉占位符.
    5) 首页“为什么选择我们”区域 6 张图
       放置位置：
       - 页面/组件：web/src/views/showcase/ShowcaseLanding.vue -> advantageCards/disadvantageCards -> .comp-card-image img
       - 资源目录建议：web/src/assets/photos/landing/why-us/
       命名建议：
       - advantage-1.jpg ~ advantage-3.jpg
       - disadvantage-1.jpg ~ disadvantage-3.jpg
       大小：
       - 前端显示：100x100（圆形裁切）；平板 80x80；移动端 70x70
       - 建议源图：600x600（1:1），最小不低于 300x300
    **事实操作**:已填充.应该替换掉占位符.
    6) 首页-合作伙伴图标（6 个）
       放置位置：
       - 页面/组件：web/src/views/showcase/ShowcaseLanding.vue -> sponsors -> .sponsor-logo img
       - 资源目录建议：web/src/assets/photos/partners/
       命名建议：
       - tencent-cloud.svg
       - bytedance.svg
       - aliyun.svg
       - huawei-cloud.svg
       - baidu-cloud.svg
       - jd-cloud.svg
       大小：
       - 前端显示容器：70x70，图标最大显示 55x55；移动端容器 55x55，图标最大 40x40
       - 建议源图：SVG 优先；若 PNG 建议 220x220（透明背景）
    **事实操作**:弄了几个开发工具的SVG图标.
- [x] 现在在数据库初始化时会触发初始数据加载的逻辑,但是现在的加载逻辑非常粗糙,且非常混杂.根据现在的代码,进行大刀阔斧的
  修改,去掉原有的混乱的数据初始化,保留管理员初始化逻辑,并提供更规范的更贴合现在的系统的初始化逻辑,且要print初始化过程,
  且在seed_data.json里面放置所有的可初始化类型的全字段实例各10个,以便于后续进行大量数据初始化.数据初始化的时候一定要结
  合系统的业务,该关联的要关联.
- [x] 解决图片加载和动画冲突的问题: 将“为什么选择我们”区块改为进入视口前预加载并异步解码图片（IntersectionObserver + lazy/async），同时移除高开销的 blur/
  backdrop-filter 动画效果，避免图片解码与入场动效争抢主线程。
- [x] 通过初始json的方法大量补充初始化数据库的数据.每种数据至少要几十条,最好要上百条.而且数据量级要加大,含有的内容要增多.
  【seed_data.json 数据补充清单】
  每类至少30-50条，最好100条：
  1. certified_users: 现有8条→扩充至30+，各类政府部门/街道办/事业单位
  2. normal_users: 现有10条→扩充至50+，普通市民姓名
  3. policy_documents: 现有5条→扩充至50+，content字段200字以上，涵盖医疗/教育/住房/就业/环保/市场监管等category
  4. opinions: 现有9条→扩充至100+，每篇文件至少5-10条，review类型需有rating(1-5)
  5. todos: 现有3条→扩充至30+，detail字段详细说明办理步骤
  6. chat_messages: 现有3条→扩充至50+，所有字段填充真实内容，original_text_mapping和chat_analysis都要有值
- [x] header上添加一个返回按钮,点击后触发浏览器的回退.
- [x] 将主页的中央文件栏以及拨动开关移动到政策推荐阅读页,初始时中央文件栏放在PolicySwipe的地方,时事热点栏放在右边的地方,他们分开且不显示切换开关.当点击左边的文章时这个中央文件栏平移到右侧于时事热点栏合并,显示切换开关,而PolicySwipe展示文章.
- [x] 政策推荐阅读界面左边增加一个UI相似的"概览"栏,展示政策推荐的标题和类别以及作者和观看量和点赞量和日期.不展示内容.在概览栏里面的上面嵌入一个轮播图栏,展示的是民意评议大厅的轮播图.
- [x] 将主页的最近解析的竖直宽度限制为最多15条.
- [x] 大刀阔斧的修改主页UI:将文件上传区变为页面正中间,跨度固定为页面的[5/12:24/13,1/4:3/4],且在最右增加卷轴,提供卷轴从左到右徐徐展开的动效.去掉"导入会话"按钮.下方的示例应该固定水平宽度为上传区的1/3,且一行只放4个.并提供一个接一个浮现的动效,浮现时里面的图片要抖一抖.而且现在每一个示例的图片应该把图片下方掩盖在示例的文字白色区的底下,形成层次感.将文件上传区的标题和副标题提供打字机动效,先打标题后打副标题.示例区点击每一个示例后都应该开始识别这个示例图片.暗黑模式下示例的白色要变成灰色.
- [x] sidebar在图标模式下的每一项在选中时应该是圆形的轮廓而不是圆角.且图标要靠中间而不是靠左.切换时应该增加流体动效.
- [x] 暗黑模式下民意大厅的气泡的背景应该是透明的.我的政策反馈和实时评议信息流及其组件都应该适应暗黑模式.
- [x] 暗黑模式下设置页面的选项文字是灰色的,应该改成白色.
- [x] 暗黑模式下发现页面的搜索框里面有灰色矩形,应该改为透明.全景政策广场及其组件应该适应暗黑模式.
- [x] 暗黑模式下登录注册页面输入的文字的地方有灰色矩形,应该把它的底色变成透明.
- [x] 将sidebar的民意大厅改为民生大厅,并修改页面的标题.避免政治错误.
- [x] Agent 插件项目化对接（2026-04-09）
  - 将 `app/agent_plugin` 配置改为由 `app/core/config.py::GlobalConfig` 统一管理，新增 `AGENT_PLUGIN_*` 环境变量。
  - 新增 `app/agent_plugin/bootstrap.py` 负责把项目全局配置注入 `agent_plugin.AgentConfig`。
  - 新增 `app/services/agent_plugin_service.py`，并接入 `app/services/agent_service.py` 运行链路（失败自动回退旧 `run_agent_graph`）。
  - 修复插件问题：移除工具中的硬编码 `user_id`、统一 `stream_run` 同步接口声明、实现检索阈值过滤、清理 README 中敏感示例。
  - 同步更新 `.env`、`README.md` `.env` 模板与 `docker-compose.yml` 环境变量映射。
  - 插件关系库存储合并到全局 `database.db`（不再使用 `agent_plugin.db`）。
  - 删除旧 Agent 链路：移除 `app/ai/agent_graph.py`、`app/services/agent_memory_service.py`，`agent_service` 改为仅走 `agent_plugin`。

- [x] 清理旧 Agent/RAG/存储链路（2026-04-09）：删除 pp/services/rag_service.py，移除 dmin/stats/chat_message 中旧 RAG 调用，/rag/* 接口降级为禁用占位返回，保留仅 plugin 的 agent 执行链路。
- [x] 下载Embedding模型,需要事先将对应的离线配置设置为在线,下载后再改成离线.
- [x] 模仿KIMI的页面布局,重构我们现在的智能体中心,智能体名字就叫做"云小圆"(CloudCycle).
  抛弃原来旧版本的结构化语义和废弃的接口,结合现在的接口来重设计UI和页面,而且像刚刚说的那样有高级感的曲线渐变
  色和反光的一个实时动态小球作为它的头像.要体现它是一个Agent,也就是说回答时要展示思考过程和调用工具,而且要有流式回答(过程和 
  工具不需要),且用户可以选择"Agent"模式和"Chat"模式,代表指定直接回答的agent还是指定直接进入循环的agent,自己改后端.
  整个页面都应该充满沉浸式的海量动态效果,并接入明暗切换.
- [x] 对Agent页面进行修改:点击进入Agent页面时,应该首先显示全屏的无边框的CloudCycleHero,且文字(标题和副标题)应该带有打字机效果,点击开始体验后,就用动效切换成仅包含文本输入框和像KIMI那样的大标题的主界面.该用户在此次登录中就不再看到CloudCycleHero了,而是直接进入主界面.默认不要显示对话历史和推理工具与轨迹,但是提供一个把手来平滑抽拉他们..然后,Agent在回答时应该用特定的框在智能体回答的地方展示思考过程和调用工具,且要有动效,思考完成后隐藏过程并流式输出,且可以点击展开思考过程来查看.所有的组件在明暗切换的时候都要平滑变换.去掉历史栏和Hero组件的明暗切换按钮.
- [x] 实现首页内容和动效极致的美化.模仿[米哈游企业首页](https://www.mihoyo.com/).
  - 实现鼠标滚轮滚动一下就下翻一面的效果.而且每次翻到这一面(无论是下翻还是从下面翻上来翻回来)都要展示动效.
  假设将现有的页面切成六份,分别是顶部主页面,一站式政策智能服务,为什么选择我们,注册机制,免费注册,合作伙伴和链接栏.那么:
  - 在顶部主页面和一站式政策智能服务之间增加单页:UI概览页,右侧三分之一展示标题和副标题和文字,左侧轮播展示软件内UI组件(多个同页面组件为一组,出现时从左向右平滑缓慢移动,离开时渐变消失),总共展示五组,每组都在中间的下面用方框展示轮播页面图标.被展示的图标应该发光.
  - 将"为什么选择我们"页面的右下角的胶囊向下移动,和上面距离上上面的距离相同.
  - 在为什么选择我们和注册机制之间增加单页:设计理念,中间是一个彩色艺术字大标题(将人们与世界衔接,引领群众响应国家号召),中间展示文字.
  - 在上面这个页面的下面再增加一页:亮点速览,下面是一个一个圆形,圆形之间有些间隔,间隔之间放箭头,且圆形和箭头的链条很长,多余的部分隐藏在外面且可以随着时间滑动.左上角书写亮点速览,中间用圆角卡片放软件设计的亮点,展示时平滑浮现并打字机效果,且提供炫酷链接导向亮点所在页面.
    现在先放四个亮点:Agent,超酷炫公共数据大屏,可视化政策扫描图谱,刷剧式无底资讯体验
  - 将顶不住页面的四个数字改成带有后端真实统计接口的.
  - 将一站式政策智能服务的ClearFlow智能体改为云小圆,副标题将AI改成AI Agent.
- [x] 实现极其酷炫的数据分析大屏.
  - 在所有之前,首先应该尝试制作像web/super-components/酷炫流光块.html这样流光的矩形组件LumineWidget,矩形应该有四个角被切割掉,流光在边缘动态流动永不停息;
    然后制作一个和它有相同流光的CentreWidget,这个块四个角被切割掉一点,而且被切割的角和角周围靠里的地方显示出发光带,四个发光带只在四个角周围一定距离,发光带也要动态变化,动态发光.
  - 可以完全删除掉ShowCaseScreen里面的占位公共数据widget,完全按照你的想法去布局这个公共数据分析大
      屏.要求:大屏中间要放一个像管理员页面底下那种可交互可缩放的中国地图,而且还要可以三维移动,有流光动效和厚度;要用web/super-
      components/数据显示塔.html展示三个数据,不同颜色.其他的所有框都应该带有极致平滑和实时动效的流光效果,包括标题也要有一个东西
      托着.不过不要使用圆角卡片形式的简约widget,而是要以繁华为主,科技分析复杂为主,展示为主.先在根目录写一个
      PUBLIC_DATA_ANALYSIS_PLAN.md,详细规划要如何制作这个页面,然后就可以开始制作这样的公共数据分析大屏了.注意,不要把一个文件弄
      得太长,建议组件化.
- [x] Agent的回答过程需要做调整:
  - 现在的问题:无论AI是agent还是chat,它都先在回答时展示思考过程和工具调用,而且还显示0条,随机思考完成后突然显示思考过程和工具调用的多条记录,完全没有体验感.
  - 要实现的目标:AI在回答时,在思考时展示一个loading的状态(用web/super-components/loader.vue),思考过程中应该逐条展示思考过程和工具调用的多条记录,如果是chat模式则不需要展示任何思考过程和工具调用栏,并且在展示时有一个逐条展示的效果(下一条思考出现时直接覆盖上一条思考),让用户有一个更好的体验感.
    且思考过程与工具调用的这个外框不应该存在,而是应该直接展示它里面的思考或工具框,后一个框迭代签一个框.
    每次登录时点击Agent页面不应该加载上一次的对话,每次进入都应该是新对话.在新对话状态下,对话框应该放在中间,当用户输入并发送后,对话框平移下来.
- [x] 将智能体头像变成一个动态变化的光滑发光圆球,且色系为珊瑚色系.
- [x] 美化智能体页面Agent回答的markdown,让它看起来不那么诡异:
    ```md
    引入 GitHub 风格样式（最快）
    在你的 HTML 中引入 github-markdown-css。

    安装：npm install github-markdown-css

    包裹：给你的 Markdown 容器加上 markdown-body 这个类名。

    HTML
    <div class="markdown-body">
      </div>
    ```
- [x] 将数据分析-我的-管理员 这三个界面适应现在的双色系结构.
- [x] 删除掉sidebar和header的三色切换,用明暗切换的逻辑直接控制他们的两色切换(不需要灰色了);
  - 然后为整个应用补充敏感色系之外的第三色:酒红珊瑚色系,采取和首页一样的色系,并提供色系切换按钮(放在明暗切换按钮右边的一个新的小圆按钮里).
- [x] 提供一个单独的下载embedding模型的脚本,并将脚本运行方法写在readme中?
- [x] 解决 Docker 部署条件下无法显示头像(显示头像破碎)的问题.
  解决方法:
  1. 前端统一新增 `web/src/utils/avatar.js`,将 `default:xxx` 默认头像值解析为 Vite 打包后的真实静态资源 URL,不再直接访问 `/src/assets/...`.
  2. `Profile.vue`、`Settings.vue`、`Header.vue`、`Sidebar.vue`、`ShowcaseHeader.vue`、`Admin.vue`、`AvatarEditor.vue` 等使用头像的页面统一改为调用该解析逻辑,避免容器部署后静态资源路径失效.
  3. `web/nginx.conf` 增加 `/media/` 代理到后端,保证用户上传头像在 Docker + Nginx 场景下也能被正常访问.
- [x] 解决 Docker 部署条件下 413 解析错误,智能体连接失败等问题.
  解决方法:
  1. `web/nginx.conf` 增加 `client_max_body_size 25m`,解决上传文件时被 Nginx 拦截导致的 413 问题.
  2. Nginx 为 `/api/` 增加更长的超时和关闭代理缓冲,降低大文件解析和长耗时请求被中断的概率.
  3. 前端 `Agent.vue` 的 WebSocket 地址统一改为 `/api/agent/ws`.
  4. Nginx 同时代理 `/api/agent/ws` 和兼容旧路径 `/agent/ws`,解决浏览器缓存旧版前端资源时 WebSocket 被 SPA 路由吞掉、导致智能体连接失败的问题.
- [x] 为部分页面进行**色系切换适配**和**响应式布局化**.如下:1.模仿"我的"页面的UI的,有:系统设置页面,发现页面的资讯列表和热
  点文件top5和全景政策广场; 2.模仿管理员页面的UI的,有:会话历史页面,我的收藏页面,办事进度中心,民生福祉大厅.不要过
  于重构,仅适配即可.
- [x] 完善图谱的逻辑:
  - [x] 应该尽可能让图谱展示最多的节点,而不是仅仅的提炼几个标题,允许大的文件产生的图谱特别大.
  - [x] 增加节点与原文对照的功能,点击节点可以在原文中高亮显示对应的内容出处,并且提供查看父节点和子节点的功能.
  - [x] 将解读结果页面的UI进行适配化处理.并将承载知识图谱和矩形文本的区域结构化为一个组件,便于在其他地方调用小窗.(也就是agent的图谱展示小窗)
  - [x] 矩形文本初始应该完全展开,且提供"知识图谱式简化版"和"原文结构化版"的切换.
  - [x] 美化一下二维知识图谱,颜色要适配全局色系并抛光,且可以提供全屏(全浏览器页面)模式.
  - [x] 解决在不点击任何节点之前点击深度筛选的任何深度都没反应的问题.
  - [x] 被选中的球应该发出动态的光泽,而不仅仅是亮色.
  - [x] 增加"个性需求推荐"的功能,自动根据个人卡片计算本文档可能对用户的价值,并将价值推荐给用户(推荐算法写入readme).
  - [x] 以上所有的更改,在Agent的可视化图谱解析小窗中也要体现.
  - [x] 解决点击节点后视角乱跳的问题,点击节点应该禁止视角进行任何移动.
  - [x] 应该将节点簇的中心绑定在相应的节点上,不能让它在上面乱飘.
- [x] 修复Agent调用查询工具返回0个结果的问题,并增加更多的Tools,如真正的解析文件和图片并展示小窗可视化图谱,全功能搜索,以及数据分析结果,特别是特定用户权限如管理员特有的数据分析等.
- [x] 增加对图文混杂型的docx和pdf的处理:采用PaddleOCR进行图文分离,对于文本部分进行正常的文本处理,对于图片部分进行OCR识别并将识别结果与文本结果进行融合,以保证图文混杂型文档的解析质量和完整性.
- [x] 更新旧式的历史记录和导出导入记录的业务,适应现在的可视化和结构化.历史有很多种,比如文档可视化解析历史,各种文章的浏览历史,各种政策的浏览历史,政策发布历史,智能体对话历史,搜索历史等.
- [x] 实现header搜索框的trie树搜索以及下拉栏,并让这个搜索框可以搜一切,包括文章,政策,以及自己的历史等.且要使用RAG进行模糊搜索.
- [x] 开始着手软件全功能搜索 - 前缀+RAG合并搜索.
- [x] 将首页第三面的"民意评论大厅",政策广场的全景政策广场的"民意评论大厅" 都改成民生大厅.
- [x] 软件UI的去AI化.
  注:以下UI调整在实现时,还应该保持其原有内部布局不变,只调整外部边框和颜色等;应该考虑响应式布局,还应该考虑在用户点击此页面时组件出现时的动效.
  - [x] 将政策大厅-政策推荐阅读的搜索框在水平方向放在中心,竖直方向减小1/3.
  - [x] 将发现页-功能中心的砖块变为相应全局色系的颜色的的多个纯色系,且受到明暗控制.并考虑响应式布局下的砖块适配,避免砖块在页面横向压缩时变成"上下参差不齐的砖墙".
  - [x] 将整个应用的loader都采用Agent的loader.
  - [x] 以主页-示例的边框(小圆角,线条)为标准(暗色系下边缘要发光),需要向其靠拢的widget:
    - 主页-最近解析
    - 主页-中央文件
    - 发现页-资讯列表的每条资讯组件
    - 办事进度中心的所有条目
  - [x] 以政策大厅-政策发布中心-发布新政务文件的边框(直角,线条)为标准(暗色系下边缘要发光),需要向其靠拢的widget:
    - 发现页-资讯列表
    - 发现页-全景政策广场及其所有条目
    - 发现页-热点文件TOP5
    - 民生大厅-热门政策TOP5
    - 民生大厅-气泡区域的边框
    - 民生大厅-实时评议信息流及其所有条目(条形+砖块),且将此区改名为民生反馈
    - 政策大厅-政策推荐阅读-政策概览的所有条目
    - 办事进度中心的widget
    - 会话历史-统一历史
    - 会话历史-承载搜索框的widget(搜索框不变)
    - 会话历史的全部条目
    - 数据分析的所有widget(除节省时间卡片之外)
    - 个人中心除了个人卡片之外的全部widget及其组件
    - 系统设置除了个人卡片之外的全部widget及其组件
  - [x] 将按钮进行差异化设计,分成3派:
    - 白色派(暂时不改):模仿web/super-components/白色按钮(变色).vue,并加入全局色系切换和明暗切换,且要在暗色系下发光.
    - 填色派(暂时不改):模仿web/super-components/填色按钮.vue,并加入全局色系切换和明暗切换,且要在暗色系下发光.
    - 特制派: "采用"指的是模仿而不是直接引用.
      - header和showcaseheader和个人中心和系统设置的退登按钮:使用web/super-components/退登按钮.vue,且不受到色系控制.
      - 所有的展开按钮或者查看全部按钮都采用web/super-components/LearnMore.vue,且受到色系切换.
      - 主页-文件上传区的三个核按钮应该采用web/super-components/GPT按钮.vue,但是将GPT图标改成相应的可视化图标,文字改成相应的功能,且受到色系切换.
      - sidebar下方的三个点那个按钮,应该采用web/super-components/About.vue,且受到色系切换.
  - [x] 修复暗色系下输入框输入文字的区域是黑色底的问题(应该为透明底):
    - 登录注册页面的输入框
    - 发现页面顶端的搜索框
    - 政策推荐阅读的搜索框
    - 历史记录页面的搜索框
  - [x] 去渐变化: 不再使用渐变,而是使用当前色系的某种颜色.(不要把下面的所有东西都弄成同一个颜色)
    - 主页-最近解析按钮,以及最近解析widget的恢复该解析按钮.
    - 主页-中央文件按钮,以及中央文件的"中华人民共和国国务院"条子.
    - 政策大厅-政策推荐-中央文件的"中华人民共和国国务院"条子应该像上面这个相同.
  - [x] 将政策推荐阅读的搜索框与下面隔开一点距离.
  - [x] 修复会话历史的每一条记录的高度消失的问题.
  - [x] 将sidebar的"..."按钮的底变成透明,将设置按钮和sidebar下面的头像变成和这个按钮一样的样式.
  - [x] 将民生大厅-热门政策top5右边的查看全部按钮在用户不动的时候的背景颜色从渐变设置为透明,当用户鼠标浮上来时再显示颜色并展示原有动效.
  - [x] 将展示首页的翻页动效从滚动-隐藏-出现 改为用户滚动后直接无缝衔接到下一面,不需要隐藏,下一面的UI应该直接从下方进入,且和下方的页面有一定的错位感,以提高层次感.
- [x] !!!!!重要: 解决审图号问题!!!!!
  - 将公共数据分析页现有的阿里云GeoJSON格式中国地图更改为地图来源： (天地图 服务中心)[https://cloudcenter.tianditu.gov.cn/administrativeDivision] 提供的GeoJSON格式中国地图,审图号: GS（2024）0650号
- [x] 手机号一键登录应该要保证即使是未注册的手机号也要可以直接登录.
- [x] 替换软件图标favicon.icon.
- [x] 展示首页的占位符替换和文字正式化:
  - 02 轮播图组真正填充,标题下的说明文字修正,每页轮播图右下角的"可继续补图"以及其文字也修正.
  - 04 给每个优势和痛点的胶囊底色变成液态玻璃,并将文字变成白色;去掉下面中间的"待补主视觉"框.
  - 05 右下角的"这里可补一张理念主视觉"卡片文字修正.
  - 06 每个卡片的中间都太空,都要放图片,四个图片的存放地点: Agent 智能体 `web/src/assets/photos/main-examples/科研论文解读.png`; 公共数据大屏 `web/src/assets/photos/discover/slide2.jpg`; 政策扫描图谱 `web/src/assets/photos/main-examples/复杂系统设计规范.png`; 刷剧式资讯体验 `web/src/assets/photos/discover/news/discover-news-05.jpg`. 然后每个卡片下面的进入体验按钮上方的那个卡牌应该删除.
  - 07 将每个卡片改成透明底色,并加上业务描述流程,避免现在过于空旷.
- [x] 让首页和政策广场的图片在加载时不要等到用户滑到哪里才开始加载.
- [x] 在政策推荐阅读页面的政策概览栏和中央文件栏的中间再加上一个新的"阅读区"栏,当点击左边的政策概览的一条时,文件展示会出现在这个阅读区栏,而不是让政策概览栏消失.
- [x] 将政策发布中心发布了新政务文件后在右边显示的"我的文件"里面的每一条去掉左边的颜色带,并将其所有的胶囊标签变成文字.
- [x] 解决办事进度中心里面点击一个待办之后待办无动于衷的问题.
- [x] 解决办事进度中心和新建待办里面全是乱码的问题.并书写为什么Codex总是反复无常的写乱码和读乱码.
- [x] 解决办事进度中心新建待办后出现一个无字框的问题.
- [x] 办事进度的新建待办的框子中,当点击了截止时间后不应该由用户自己手动输入,而是应该出现一个日期选择框.
- [x] 解决政策推荐阅读页面中,文件不能点赞的问题.并在点赞旁边加一个收藏按钮.
- [x] 删除管理员页面的公户IP地理分布区.
- [x] 将主页上传区的三个按钮之中的"截图识别"换成"拍照识别".
- [x] 把点击退出登录时的alert变成一个真正的消息框.
- [x] 暗色系下:
  - 主页-示例的每一个卡片的胶囊的底色都应该随全局色系改变.
  - 云小圆页面的标题和副标题下的两个胶囊的文字应该变成白色,且思考过程框和聊天框的颜色应该是随色系决定,而不是简单的浅红.
- [x] 登录注册页面的文本输入框里面现在会出现黑色长方形,应该把输入框的这个矩形给变成透明底色.
- [x] 在doc/作品演示演讲稿.txt中书写作品演示视频全流程,5分钟左右,要求的是:遍历项目的主要功能,突出项目的技术亮点,亮点处可选描述本产品解决的难点.
